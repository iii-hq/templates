name: {{worker_name}}
runtime:
  # Base OCI image used as the worker rootfs.
  base_image: docker.io/library/rust:slim-bookworm
scripts:
  setup: cargo install cargo-watch
  install: cargo build
  start: cargo watch -x run
