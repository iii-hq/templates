# Provider credentials for the agent. Add the key for the provider you use, then
# start Compose. llm-router reads this file (see worker-compose.yaml), and the
# provider worker you add reaches its API with the key.
#
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
