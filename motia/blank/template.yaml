# Quickstart template configuration
name: Blank
description: A blank template useful for starting a new Motia project
version: "0.1.0"
min_iii_version: "0.11.0"

# Languages that can optionally be included
optional:
  - typescript
  - javascript
  - python

# Explicit file manifest for fetching
files:
  # Root config files
  - .env
  - motia.config.ts
  - motia-workbench.json
  - package.json
  - requirements.txt

  # Step files - filtered by language selection
  - src/typescript.step.ts
